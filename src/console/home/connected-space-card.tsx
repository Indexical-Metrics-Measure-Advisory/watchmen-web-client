import React from 'react';
import { ConnectedSpace } from '../../services/console/connected-space-types';
import { ConnectedSpaceCardContainer } from './widgets';

export const ConnectedSpaceCard = (props: {
	connectedSpace: ConnectedSpace;
}) => {
	return <ConnectedSpaceCardContainer>

	</ConnectedSpaceCardContainer>
}