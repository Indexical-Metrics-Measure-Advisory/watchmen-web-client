import styled from 'styled-components';

export const PipelineEditor = styled.div.attrs({ 'data-widget': 'pipeline-editor' })`
	display        : flex;
	position       : relative;
	flex-grow      : 1;
	flex-direction : column;
`;
